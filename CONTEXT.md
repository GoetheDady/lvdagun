# 驴打滚

个人 AI 管家应用,本机自托管形态:常驻本机的 Agent Hub + 浏览器客户端。本上下文覆盖产品领域概念:架构角色、模型服务配置、设置页与首次配置向导。

## Language

**驴打滚**:
本项目及其个人 AI 管家的正式中文名称。品牌视觉优先表达同名北京小吃,而非驴这一动物或通用 AI 符号;气质活泼可爱,面向中文开源用户。
_Avoid_: lvdagun、驴吉祥物、机器人助手

**Agent Hub**:
常驻本机的 Agent 内核,负责模型对话、工具执行与上下文管理。V0 承载于本机 Web 服务进程。
_Avoid_: 内核、服务端、后台

**本地服务**:
常驻本机的 Web 服务进程,承载 Agent Hub 与模型会话,提供本地 HTTP 接口,由 CLI 启动/停止。
_Avoid_: server、后台进程

**客户端**:
连接 Hub 的接入端,无状态,只负责交互与展示。V0 为浏览器页面;Electron 壳后置,仅作窗口与托盘。
_Avoid_: 前端、界面

**Hub 连接**:
客户端与 Agent Hub 建立并完成协议初始化的应用级通信通道。Hub 连接成功不表示当前会话空闲或会话模型可用。
_Avoid_: 就绪、会话连接、模型连接

**Agent 运行**:
Agent Hub 接受一条用户提示后,完成全部模型调用、工具执行、自动重试及收尾的一次处理过程。同一会话同一时间只允许一次 Agent 运行。
_Avoid_: 请求、回复生成

**运行标记**:
Agent 运行期间向用户表达当前可判断处理阶段的瞬时状态。它随 Agent 运行结束而消失,不属于产品会话历史。
_Avoid_: Todo 状态、工具卡片、完成记录、全局运行状态

**消息提交**:
用户明确将当前输入交给 Agent Hub 的动作。输入法候选确认只改变输入内容,不属于消息提交。
_Avoid_: 输入确认、发送完成

**会话执行计划**:
Agent 为完成当前会话中的复杂目标而维护的结构化计划步骤集合。它跨 Agent 运行、停止和恢复持续存在,直到 Agent 为新的目标明确清空或替换;用户只查看并通过消息调整目标。
_Avoid_: Todo、待办清单、用户任务列表、单次运行计划

**计划步骤**:
会话执行计划中一个可独立推进和确认结果的工作项,由 Agent 创建并维护状态。
_Avoid_: Todo item、待办事项、用户任务

**助手回复**:
一次 Agent 运行从开始处理到完全结束所产生的完整展示单元,可以包含多次模型调用、思考、工具运行、重试提示和普通文本。
_Avoid_: AssistantMessage、模型响应、单次调用、Pi turn

**原生流式展示**:
助手回复尚未完成时,用户直接看到 Agent Hub 已产生的最新内容;展示节奏忠实于实际生成进度,不人为模拟逐字速度或淡入。
_Avoid_: 打字机效果、逐字动画、流式动画

**提示准入**:
Agent Hub 对用户提示完成前置校验并确认接管的时刻。提示准入只确认驴打滚已接受该提示,不表示对应的 Agent 运行已经结束或成功。
_Avoid_: 发送完成、运行完成、回复成功

**并行 Agent 运行**:
不同会话各自同时进行 Agent 运行的状态。同一会话仍然最多只有一个 Agent 运行;并行会话共享本机环境,工具副作用不相互隔离。
_Avoid_: 多 Agent、多 Runtime、全局并行

**调整方向**:
在 Agent 运行期间补充指令,使其并入当前 Agent 运行并影响后续处理。
_Avoid_: steer、纠偏消息、立即消息

**排队**:
在 Agent 运行期间提交消息,等待当前处理路径自然结束后再继续处理,不用于改变当前处理路径。
_Avoid_: followUp、后续消息

**待处理消息**:
当前会话中已被接受、由驴打滚等待移交给 Agent 处理的消息集合。消息被调整方向或按排队顺序移交后,不再属于待处理消息。
_Avoid_: queue_update、队列状态

**内置 Extension**:
随驴打滚发布并由 Agent Hub 显式加载的 Pi Extension,用于实现确定性产品能力或干预 Agent 生命周期。
_Avoid_: Pi SDK 补丁、外部 Extension、插件

**外部 Extension**:
由用户提供并通过资源目录发现的 Pi Extension,其加载与内置 Extension 相互独立。
_Avoid_: 内置 Extension、插件

**工具运行**:
Agent 为完成任务而调用一个工具并取得结果的过程。一次 Agent 运行可以包含一个或多个串行或并行的工具运行。
_Avoid_: 工具消息、工具事件

**上下文压缩**:
Agent Hub 将较早的对话归纳为摘要,以控制后续模型调用所需上下文长度的过程。上下文压缩不会清空用户看到的历史。
_Avoid_: 清空历史、删除消息

**会话**:
用户与 Agent Hub 之间一段独立且持久保存的对话上下文。会话在本地服务停止并重新启动后仍然存在。
_Avoid_: 聊天、对话线程

**执行历史**:
Agent 为延续上下文和执行原生会话控制而保留的结构化记录,包含提示、模型消息、工具结果及分支关系。
_Avoid_: 产品会话历史、元数据、聊天记录

**产品会话历史**:
驴打滚面向客户端展示与恢复的完整会话记录,以助手回复等产品语义组织执行过程中产生的内容和状态。
_Avoid_: 执行历史、Pi 消息数组、事件日志

**会话列表**:
本机尚未归档的会话集合,包含当前正在查看的会话和先前创建的会话。
_Avoid_: 历史会话、会话历史

**当前会话**:
客户端当前选择并展示的会话。不同客户端可以独立选择会话;多个客户端选择同一会话时共享消息与 Agent 运行状态。
_Avoid_: 全局会话、活动会话

**会话快照恢复**:
客户端重新连接当前会话时,先用 Agent Hub 的完整历史、当前部分回复和运行状态校准展示,再继续接收新内容。恢复已有内容不重新播放生成过程,也不补演断线期间的瞬时进度。
_Avoid_: 事件重放、断点续传、重新加载历史

**新会话**:
不携带既有消息的新建会话。创建新会话不表示删除已有会话。
_Avoid_: 清空会话、重置聊天

**会话标题**:
用于在会话列表和当前会话中辨识会话的简短名称。展示优先级依次为手动标题、自动标题、首条用户消息和“新对话”。
_Avoid_: 会话名称、聊天标题

**自动标题**:
首轮成功的 Agent 运行结束后由会话模型根据首条用户提示和最终回答生成的中文短标题,每个会话最多自动生成一次。
_Avoid_: 默认标题、首条消息

**手动标题**:
用户明确设置的会话标题,一旦设置便不再被自动标题覆盖。
_Avoid_: 自定义名称、会话别名

**切换会话**:
从会话列表中选择一个已有会话继续交互。切换会话不会复制、合并或删除任何会话。
_Avoid_: 恢复历史、打开历史

**会话分支**:
同一会话从历史节点产生的不同后续路径。原路径不会因产生新分支而删除,任一时刻只有一条路径作为当前分支。
_Avoid_: 新会话、历史副本

**会话回退**:
在当前会话中选择较早的历史节点作为继续位置。回退后的新消息形成会话分支,被离开的后续路径仍可重新选择。
_Avoid_: 撤销消息、删除历史

**派生会话**:
从现有会话的一段历史路径创建的独立新会话。源会话保持不变,派生会话创建后拥有独立的标题、模型和后续上下文。
_Avoid_: 会话分支、复制全部会话、fork、clone

**分叉为新会话**:
用户从一条助手回复创建派生会话的操作。源会话保持不变,创建成功后发起操作的客户端进入派生会话。
_Avoid_: 分叉、fork、clone、新会话

**编辑并重发**:
用户在原位置修改当前分支最后一条用户消息并再次发送的操作。发送被接受后形成新的当前分支,原分支保留但默认不展示。
_Avoid_: 修改消息、覆盖历史、派生会话

**归档会话**:
将暂时不用的会话移出会话列表并保留其全部内容。
_Avoid_: 删除会话、隐藏会话

**删除会话**:
永久移除会话及其本地数据,不可恢复。
_Avoid_: 归档会话、移出列表

**思考等级**:
控制模型在后续调用中投入多少推理能力的等级。可选等级由当前模型决定,不支持思考的模型只有关闭等级。
_Avoid_: 推理开关、思考模式

**会话模型**:
当前会话为后续 Agent 运行选定的大模型。各会话可以独立选择;切换不会改变其他会话或正在进行的 Agent 运行。
_Avoid_: 全局模型、当前模型

**可用模型**:
Agent Hub 已具备有效访问凭据、可以选作会话模型的大模型。没有有效凭据的模型不属于可用模型。
_Avoid_: 所有模型、模型目录

**Provider**:
提供大模型 API 的服务商,以 SDK 内置 id 标识(如 anthropic、openai、deepseek)。
_Avoid_: 平台、渠道、厂商

**模型服务配置(ModelSettings)**:
持久化的模型接入配置,由已配置 Provider 凭据列表与默认模型组成;每个 Provider 至多一条凭据条目。配置好凭据后,该 Provider 名下全部模型均可用,条目本身不绑定具体模型。
_Avoid_: 设置项、config、单条模型配置

**Provider 凭据**:
一条 Provider 访问配置,由 Provider id 与 API Key 组成;本地模型等场景 Key 允许为空。凭据在设置页增删改,与首次配置向导相互独立。
_Avoid_: 模型配置、账号

**默认模型(DefaultModel)**:
新会话的初始会话模型,也是会话模型失效时的回退目标,由用户在设置页显式指定。
_Avoid_: 当前模型、选中模型(会话内的切换是会话模型,不是默认模型)

**设置页**:
承载产品配置的次页面,左侧为分区导航,右侧为当前分区详情;V0 分区为模型服务与关于。首次配置引导不在设置页。
_Avoid_: 配置向导页、向导

**模型服务(分区)**:
设置页中管理模型接入的分区,含默认模型选择与已配置 Provider 列表;新建与编辑在独立的 Provider 编辑页进行。
_Avoid_: 模型配置、Provider 管理

**配置向导**:
首次访问的三步引导:选 Provider → 填 API Key 并测试连接 → 选 Model。完成后写入模型服务配置,所选模型成为默认模型;后续配置修改在设置页进行,向导不再出现。
_Avoid_: 初始化流程、onboarding

**测试连接**:
用所选 Provider + API Key 发起最小模型请求(1 token,10 秒超时),验证凭证与网络链路可用。所用模型仅为本次验证临时选择,不随凭据持久化。
_Avoid_: 连通性检查、ping

**已配置 / 未配置**:
存在至少一条 Provider 凭据即为已配置(apiKey 允许为空);未配置时访问进入配置向导。
