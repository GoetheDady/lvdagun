# 无审批启用 Pi 默认工具

为完整提供 Pi 的 Agent 能力,生产会话启用 Pi `0.84.1` 默认的 `read`、`bash`、`edit`、`write` 工具,不增加 Web 审批或沙箱。工具以用户主目录为工作目录,但绝对路径不受该目录限制,最终权限等同于本地服务进程所属的 macOS 用户;这意味着模型可以读取或覆盖该用户可访问的文件并执行任意本机命令。当前明确接受这一风险,以保持工具行为与 Pi 默认能力一致。
