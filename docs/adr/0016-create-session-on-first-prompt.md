# 会话在首条消息提交时创建

「新建会话」点击后只进入客户端草稿态，不再立即调用 `session/create`；会话在用户提交首条消息时才创建，Pi 恢复默认的"首条消息才写 JSONL"行为，Hub 不再预写会话 header。这从源头消除两类垃圾空会话：点击后不发送的遗留会话，以及连点造成的重复空会话。`session/create` 同时接受可选的初始会话模型，草稿态的选择随创建一次生效，指定的模型不可用时回退默认模型并提示。创建成功但发送失败时保留该会话供重试，不做"创建-删除"回滚。本决策取代 ADR-0006 中"立即写入 JSONL 使空会话进入会话列表"的部分。
