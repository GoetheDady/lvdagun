/**
 * 设置页(次页面):模型选择、API Key 状态(掩码显示)、关于。
 * 与配置向导共享同一套配置。
 *
 * @returns 设置页元素
 */
function SettingsPage(): React.JSX.Element {
  return <main className="flex min-h-screen items-center justify-center p-6">设置</main>;
}

export default SettingsPage;
