/**
 * 配置向导页(仅首次启动出现):选 provider → 填 Key(测试连接)→ 选 model。
 * 当前为占位内容,后续实现三步流程。
 *
 * @returns 向导页元素
 */
function WizardPage(): React.JSX.Element {
  return <main className="flex min-h-screen items-center justify-center p-6">配置向导</main>;
}

export default WizardPage;
